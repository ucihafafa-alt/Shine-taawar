function board(){let province=localStorage.getItem(DB+'_filter')||'';let rows=[...S.users].filter(u=>!province||u.province===province).sort((a,b)=>(b.score||0)-(a.score||0)).slice(0,100).map((u,i)=>`<tr><td>${i+1}</td><td>${u.name}<div class="muted small">${u.province||'-'} · Lv ${u.level}</div></td><td>${u.xp||0}</td><td><b>${u.score||0}</b></td></tr>`).join('');return `<h2>Чансааны самбар</h2><div class="card"><label>Аймгаар шүүх</label><select onchange="localStorage.setItem(DB+'_filter',this.value);render()"><option value="">Бүгд</option>${provinces.map(p=>`<option ${province===p?'selected':''}>${p}</option>`).join('')}</select><br><br><table class="table"><tr><th>#</th><th>Нэр</th><th>XP</th><th>Оноо</th></tr>${rows||'<tr><td colspan="4">Одоогоор хэрэглэгч алга.</td></tr>'}</table></div>`}
